# LogDir
# --
	Dir("/home/roar/CFT/logs").protect
/LogDir

# Check if file in log dir newer than 1 minute
# --
	found=false
	oneMinuteAgo=Date.sub(Date.Duration.minutes(1))
	limit=oneMinuteAgo.get  # millis
	LogDir.files->f
		if (f.lastModified > limit) found=true
	|
	found
/HasNewLog


# Returns CIFSFile
# --
    url = Vault:SessionPropertyGet("Enter URL, on format smb://host/share/path/file.txt")

    user = Vault:SessionPropertyGet("User name")
    password = Vault:SessionSecretGet("Password")
    
    context=Std.Util.CIFS.CIFSContext(null, user, password)
    Std.Util.CIFS.CIFSFile(url,context)
/CIFSFile

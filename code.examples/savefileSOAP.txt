# Readme
# --
<<< EOF
2026-03 Status
--------------

The code to process WSDL files is FLAWED, and can not be trusted for anything
critical. 

It has a few known bugs/weaknesses:

	- resolving targetNamespace for schema and wsdl elements is completely "ad hoc"
	- elements depending on sub-content defining the type do not work

Still it was good enough to validate a competing implementation written in Java
by AI, as I was able to validate some 200 SOAP endpoints, comparing the XML:PP output
for the expanded namespace format ("Clark notation"), which makes the data independent
of the namespace prefixes. It failed for about 15, and those failures were massive,
with the output from this script lacking most or all message content. I tend to trust
the AI got it right.

To process a wdsl file, see functions test() and show() - or create a separate
script calling ProcessWsdlFile()

>>> EOF
/Readme


# Process WSDL file and produce output
# --
	P(1) as File => f
	P(2,false) as boolean => debugMode
	P(3) as Dir? => targetDir  # where we write resulting XML files
	
	
	dataStore=SOAP:DataStore
	
	wsdl=SOAP:WsdlDocument
	wsdl.init(f,dataStore)


	# (DEBUG) while developing, find forgotten attributes
	#SOAP:ShowUnresolvedAttributes(wsdl)

	# (DEBUG) show attributes in nodes that both have namespace and not
	#SOAP:ShowAttributesMissingResolve(wsdl)

	# (DEBUG) attributes with simple values
	#SOAP:ShowNonNamespacedAttributes(wsdl)


	
	# update attribute values for different kinds of XML nodes, deciding'
	# which "inherit" targetNamespace, and which don't
	SOAP:UpdateAttributeValuesNamespaces(wsdl)

	#XML:Explore(wsdl.rootNode)
	#error("STOP")

	Lib:Header("Creating templates")
	s="Autogenerated " + Date.setFormat("yyyy-MM-dd").fmt
	rawOutput = List(
		"# " +s, 
		"# " + "-".times(s.length),
		""
	) + dataStore.createTemplates

	if (targetDir) targetDir.file("raw.txt").create(rawOutput)

	CreateFormattedOutput(rawOutput, debugMode, targetDir)
	
	# -----------------



	##XML:PP(wsdl.rootNode).print

	#SOAP:ShowAllNodeTags(wsdl)
	
/ProcessWsdlFile


# Test in debug mode
# --
	P(1) as File => file
	ProcessWsdlFile(file,true)
/test

	
# Show without debug
# --
	P(1) as File => file
	ProcessWsdlFile(file,false)
/show


# WSDL namespace on internal format
# --
	P(1,"") => tag
	"{http://schemas.xmlsoap.org/wsdl/}"+tag
/NS_WSDL

# SOAP namespace on internal format
# --
	P(1,"") => tag
	"{http://schemas.xmlsoap.org/wsdl/soap/}"+tag
/NS_SOAP

# XSD namespace on internal format
# --
	P(1,"") => tag
	"{http://www.w3.org/2001/XMLSchema}"+tag
/NS_XSD


NS_WSDL("definitions")
/WSDL_DEFINITIONS

NS_XSD("schema")
/XSD_SCHEMA

NS_XSD("import")
/XSD_IMPORT

# Return ComplexType object or null
# --
	P(1) as &WsdlDocument => wsdlDocument
	P(2) as String => typeName

	if (typeName.startsWith(XSD_SCHEMA)) error("Can not look up " + XSD_SCHEMA + " type: " + typeName)

	Inner{
		wsdlDocument.rootNode.allSubNodesByTag(SOAP:NS_XSD("complexType"))->type
			assert(type.attrGet("name")==typeName)
			out(type)
	} _=> typeList
	if (typeList.length > 1) {
		println("typeName " + typeName + " matches " + typeList.length + " complexType elements")
		XML:Explore(wsdlDocument.rootNode)
		error("Terminating")
	}
	if (typeList.empty) null else typeList.first
/LookupComplexTypeByName


# Return SimpleType object or null
# --
	P(1) as &WsdlDocument => wsdlDocument
	P(2) as String => typeName

	if (typeName.startsWith(XSD_SCHEMA)) error("Can not look up " + XSD_SCHEMA + " type: " + typeName)

	Inner{
		wsdlDocument.rootNode.allSubNodesByTag(SOAP:NS_XSD("simpleType"))->type
			assert(type.attrGet("name")==typeName)
			out(type)
	} _=> typeList
	if (typeList.length > 1) {
		println("typeName " + typeName + " matches " + typeList.length + " simpleType elements")
		XML:Explore(wsdlDocument.rootNode)
		error("Terminating")
	}
	if (typeList.empty) null else typeList.first
/LookupSimpleTypeByName


# Return SimpleType or ComplexType object, or fail if not found
# --
	P(1) as &WsdlDocument => wsdlDocument
	P(2) as String => typeName

	type=LookupSimpleTypeByName(wsdlDocument, typeName)
	if (type==null) type=LookupComplexTypeByName(wsdlDocument, typeName)
	if (type==null) {
		println("Unknown type name: " + typeName)
		XML:Explore(wsdlDocument.rootNode)
		error("terminating")
	}
	type=ResolveRef(wsdlDocument, type)
	type
/LookupType


# Is a type a system type?
# --
	P(1) as String => typeName

	typeName.startsWith(SOAP:NS_XSD)

/IsSystemType


# Deconstruct {xxx}yyy type string, returns SymDict(namespace,prefix,simpleName)
# --
	P(1) as String => value

	value.after("{").before("}") => namespace
	namespace.split("/").last => prefix
	value.after("}") => simpleName
	SymDict(namespace,prefix,simpleName)
/DeconstructType

# Look for type node as sub-content under the element
# --
	P(1) as &XMLNode => element
	typeNode=null

	element.subNodes->tnode
		if (tnode.tagName==SOAP:NS_XSD("simpleType") || tnode.tagName==SOAP:NS_XSD("complexType")) {
			typeNode=tnode
		}
		break(typeNode)
	|
	typeNode

/GetEmbeddedType


# Resolve ref attribute
# --
	P(1) as &WsdlDocument => wsdlDocument
	P(2) as &XMLNode => node

	ref=node.attrGet("ref")
	if (ref != null) Inner{
		tag="}"+node.tagName.after("}")

		foundNode=null
		wsdlDocument.rootNode.allSubNodes->node
			assert(node.tagName.endsWith(tag))
			assert(node.attrGet("name")==ref)
			foundNode=node
			break(foundNode)
		|

		if (foundNode.attrGet("ref") != null) {
			ResolveRef(wsdlDocument, foundNode.attrGet("ref"))
		} else {
			foundNode
		}
	 } else {
		# no change
		node
	} _=> result
	if (result != node) {
		println("ResolveRef " + node.tagName + " => " + result.tagName)
	}
	result
/ResolveRef


	

# Formatted output (indents)
# --
	P(1) as List => lines
	P(2,false) as boolean => debugMode
	P(3) as Dir? => targetDir

	targetFile=null

	buffer=List

	indent=0
	concat=false  # force next line to be added to last line (for processing ${VALUE} merge codes
	lines->line
		if (targetDir != null && line.trim.startsWith("#operation")) {
			if (targetFile != null) {
				#println(targetFile.path)
				targetFile.create(buffer)
			}
			buffer=List
			targetFile=targetDir.file(line.after(" ").trim+".xml")
		} else if (line.trim=="#>>>") {
			indent = indent + 4
		} else if (line.trim=="#<<<") {
			indent = indent - 4
			if (indent < 0) indent=0
		} else if (line.trim.startsWith("#")) {
			if (debugMode) buffer.add(" ".times(indent)+"<!-- DEBUG: " + line.trim.after("#") + " -->")
		} else if (line.trim=="${VALUE}") {
			lastLine=buffer.last + line.trim
			buffer.removeLast.add(lastLine)
			concat=true
		} else if (concat) {
			lastLine=buffer.last + line.trim
			buffer.removeLast.add(lastLine)
			concat=false
		} else {
			buffer.add(" ".times(indent) + line)
		}
	|
	if (targetFile != null) {
		#println(targetFile.path)
		targetFile.create(buffer)
	} 

	if (!targetDir) buffer.print
		
/CreateFormattedOutput



# Data store
# --
	self.wsdlDocuments = List
	self.schema = List
	
	self.addWsdlDocument = Lambda{
		P(1) as &WsdlDocument => x
		println("Adding WsdlDocument")
		self.wsdlDocuments.add(x)
	}
	
	self.addSchema = Lambda{
		P(1) as &Schema => s
		println("Adding Schema")
		self.schema.add(s)
	}

	self.createTemplates = Lambda{
		OutputGenerator:Generator => root
		
		self.wsdlDocuments->wsdlDocument
			wsdlDocument.rootNode.subNodes(SOAP:NS_WSDL("portType"))->pt
				portType=ResolveRef(wsdlDocument,pt)
				root.out("# ----------------------------------")
				root.out("# portType: " + portType.attrGet("name"))
				root.out("# ----------------------------------")
				portType.subNodes(SOAP:NS_WSDL("operation"))->op
					operation=ResolveRef(wsdlDocument,op)
					gen=root.sub("operation")
					gen.out("")

					opName=operation.attrGet("name")
					gen.out("#operation " + opName)
										
					{
						operation.subNodes(SOAP:NS_WSDL("input"))->inp
							input=ResolveRef(wsdlDocument, inp)
							gen.out("")
							gen.out("#input message=" + input.attrGet("message") + " name=" + input.attrGet("name"))
							gen.out('<!-- type="input" op="' + opName + '" message="' + input.attrGet("name") + '" -->')
							
							self.createMessageTemplate(gen, wsdlDocument, input.attrGet("message"))
					}
					{
						operation.subNodes(SOAP:NS_WSDL("output"))->outp
							output=ResolveRef(wsdlDocument, outp)
							gen.out("")
							gen.out("#output message=" + output.attrGet("message") + " name=" + output.attrGet("name"))
							gen.out('<!-- type="output" op="' + opName + '" message="' + output.attrGet("name") + '" -->')

							self.createMessageTemplate(gen, wsdlDocument, output.attrGet("message"))
					}
					{
						operation.subNodes(SOAP:NS_WSDL("fault"))->flt
							fault=ResolveRef(wsdlDocument, flt)
							gen.out("")
							gen.out("#fault message=" + fault.attrGet("message") + " name=" + fault.attrGet("name"))
							gen.out('<!-- type="fault" op="' + opName + '" message="' + fault.attrGet("name") + '" -->')
							
							self.createMessageTemplate(gen, wsdlDocument, fault.attrGet("message"))
					}

		| 
		root.produceOutput
	}

	self.createMessageTemplate = Lambda{
		P(1) as &Generator => gen
		P(2) as &WsdlDocument => wsdlDocument
		P(3) as String => messageId
		
		gen=gen.sub("message")
		gen.out('<envelope:Envelope')

		gen.out("#>>>")
		gen.sub("namespaces", OutputGenerator:ListOutputUnique).out('xmlns:envelope="http://schemas.xmlsoap.org/soap/envelope/"')
		gen.out(">")
		gen.out("#<<<")


		gen.out("#>>>")
		gen.out("<envelope:Header/>")
		gen.out("<envelope:Body>")
		
		messageTag=SOAP:NS_WSDL("message")

		wsdlDocument.rootNode.allSubNodesByTag(messageTag)->m
			assert(m.attrGet("name")==messageId)
			out(m)
		| _=>messages

		error(messages.length==0, "No " + messageTag + " with name=" + messageId + " found")
		error(messages.length>1, "More than one " + messageTag + " found")

		message=ResolveRef(wsdlDocument,messages.first)
		message.subNodes(SOAP:NS_WSDL("part")) -> prt
			part=ResolveRef(wsdlDocument,prt)
			elementName=part.attrGet("element")

			if (elementName==null) {
				println("Element name is null")
				XML:Explore(element)
				error("terminating")
			}

			dtype=DeconstructType(elementName)

			gen.getParent("message").getSubGenerator("namespaces").out("xmlns:" + dtype.prefix + '="' + dtype.namespace + '"')
			gen.out("#>>>")
			gen.out("# message part name=" + part.attrGet("name") + " element=" + elementName)
			gen.out("<" + dtype.prefix + ":" + dtype.simpleName + ">")

			Inner{
				wsdlDocument.rootNode.allSubNodesByTag(SOAP:NS_XSD("element"))->element
					assert(element.attrGet("name")==elementName)
					out(element)
			} _=> elementList
			if (elementList.length != 1) {
				println(elementName + " matches " + elementList.length + " elements")
				XML:Explore(wsdlDocument.rootNode)
				error("terminating")
			}

			# Must reverse full name spaces to prefixes (stored in each node under nsPrefix dictionary field

			self.createMessageElementTemplate(gen, wsdlDocument, ResolveRef(wsdlDocument,elementList.first))

			gen.out("</" + dtype.prefix + ":" + dtype.simpleName + ">")
			gen.out("#<<<")
			
		|
		gen.out("</envelope:Body>")
		gen.out("#<<<")
		gen.out("</envelope:Envelope>")

	}

		

	self.createMessageElementTemplate = Lambda{
		P(1) as &Generator => gen
		P(2) as &WsdlDocument => wsdlDocument
		P(3) as &XMLNode => element
		element->e
			elementName=e.attrGet("name")
			typeName=e.attrGet("type")

			gen.out("#>>>")
			gen.out("# message element " + element.tagName + " name=" + elementName + " type=" + typeName)
			typeNode=null
			if (typeName==null) {
				typeNode=GetEmbeddedType(element)
				if (typeNode==null) {
					error("Element " + element.tagName + " name=" + elementName + " no type attribute and no embedded type")
				}
			} else {
				typeNode=LookupType(wsdlDocument,typeName)
			}

			if (typeNode.tagName.endsWith("complexType")) {
				self.createComplexTypeTemplate(gen, wsdlDocument, typeNode)
			} else {
				self.createSimpleTypeTemplate(gen, wsdlDocument, typeNode)
			}
			gen.out("#<<<")
			
	}

	self.createComplexTypeTemplate = Lambda {
		P(1) as &Generator => gen
		P(2) as &WsdlDocument => wsdlDocument
		P(3) as &XMLNode => complexType

		sequenceTag=SOAP:NS_XSD("sequence")
		
		gen=gen.sub("complexType")
		
		# note that for empty messages like NOOP there is no sequence nor elements, so no checks here

		complexType.subNode(sequenceTag)->sequence
			gen.out("# sequence")
			sequence.subNodes(SOAP:NS_XSD("element"))->e
				element=ResolveRef(wsdlDocument,e)
				name=element.attrGet("name")
				type=element.attrGet("type")

				if (type==null) {
					println("INFO: element type null, looking up typeNode under element")
					typeNode=GetEmbeddedType(wsdlDocument.rootNode)
					type=typeNode.attrGet("type")					
				}

				if (type==null) {
					println("createComplexTypeTemplate: no type found")
					XML:Explore(element)
					error("terminating")
				}

				if (name==null) {
					println("element name is null")
					XML:Explore(wsdlDocument.rootNode)
					error("terminating")
				}
				dtype=DeconstructType(name)

				gen.getParent("message").getSubGenerator("namespaces").out("xmlns:" + dtype.prefix + '="' + dtype.namespace + '"')
				gen.out("#>>>")
				gen.out("# complexType: element name=" + name + " type=" + type)

				minOccurs=element.attrGet("minOccurs","-")
				maxOccurs=element.attrGet("maxOccurs","-")  # can be "unbounded" or some number
				
				#gen.out('<!-- for="' + dtype.prefix + ':' + dtype.simpleName + '" minOccurs="' + minOccurs + '" maxOccurs="' + maxOccurs + '" -->')
				gen.out("<" + dtype.prefix + ":" + dtype.simpleName + ">")


				# Look up type, unless soap system type
				if (IsSystemType(type)) {
					gen.out("#>>>")
					gen.out("${VALUE}")
					gen.out("#<<<")
				} else {
					LookupType(wsdlDocument,type) => typeObject

					if (typeObject.tagName.endsWith("complexType")) {
						self.createComplexTypeTemplate(gen,wsdlDocument,typeObject)
					} else {
						self.createSimpleTypeTemplate(gen,wsdlDocument,typeObject)
					}

				}

				gen.out("</" + dtype.prefix + ":" + dtype.simpleName + ">")
				gen.out("#<<<")
	}	


	self.createSimpleTypeTemplate = Lambda {
		P(1) as &Generator => gen
		P(2) as &WsdlDocument => wsdlDocument
		P(3) as &XMLNode => simpleType

		simpleType.subNodes(SOAP:NS_XSD("restriction"))->restriction
			base=restriction.attrGet("base")
			gen.out("#>>>")
			gen.out("# simpleType base=" + base)
	
			patterns = Inner{
				restriction.subNodes(SOAP:NS_XSD("pattern"))->pattern 
					out(pattern.attrGet("value"))
			}

			enumValues = Inner{
				restriction.subNodes(SOAP:NS_XSD("enumeration"))->enum
					out(enum.attrGet("value"))
			}

			enumString=if(enumValues.empty,null," " + enumValues.concat("|"))

			{
				simpleType.subNodes(SOAP:NS_XSD("annotation"))->annotation
					annotation.subNodes(SOAP:NS_XSD("documentation"))->documentation
						docString=documentation.getText.concat(" ").trim

						if (docString.length>0) 
							gen.out("#doc: " + docString)
						if (patterns) 
							gen.out("#patterns: " + patterns.concat(" "))
						if (enumString) 
							gen.out("#enumeration: " + enumString)
			}

			if (!IsSystemType(base)) {
				LookupType(wsdlDocument,base) => typeObject

				if (typeObject.tagName.endsWith("complexType")) {
					self.createComplexTypeTemplate(gen,wsdlDocument,typeObject)
				} else {
					self.createSimpleTypeTemplate(gen,wsdlDocument,typeObject)
				}
			} else {
				# look up name from parent ComplexType which 
				gen.out("${VALUE}")
			}


			gen.out("#<<<")
				
	}

	
/class DataStore


# WSDL file parser
# --
	self.sourceFile=null
	self.portTypes=Dict
	self.rootNode=null
	
	self.init = Lambda{
		P(1) as File => f
		P(2) as &DataStore => dataStore

		dataStore.addWsdlDocument(self)
		
		self.sourceFile=f
		XML:Parse(f.read).first => root
		XML:ProcessNamespaces(root)

		if (root.tagName != SOAP:WSDL_DEFINITIONS) {
			error("WSDL root element should be of type " + SOAP:WSDL_DEFINITIONS)
		}

		self.rootNode=root
		self.targetNamespace=root.attrGet("targetNamespace")

		root.subNodes(SOAP:NS_WSDL("types"))->wt
			# inline schema
			Inner {
				wt.subNodes(SOAP:XSD_SCHEMA)->schemaNode
					s=SOAP:Schema
					s.processSchemaNode(schemaNode, f, dataStore)
			}
		|
		
	}

/class WsdlDocument



# Schema processing, main function is processSchemaNode with takes an XMLNode as parameter
# (Which must have tag SOAP:XSD_SCHEMA)
# --
	self.dataStore=null
	self.schemaNode=null
	self.sourceFile=null

	# This function gets called from the Wsdl parser, and when processing imports (below)
	# --
	self.processSchemaNode = Lambda{
		P(1) as &XMLNode => schemaNode
		P(2) as File => sourceFile  
		P(3) as &DataStore => dataStore
		
		self.dataStore=dataStore
		self.dataStore.addSchema(self)

		error(schemaNode.tagName != SOAP:XSD_SCHEMA, "Not an " + SOAP:XSD_SCHEMA + " node")

		self.schemaNode = schemaNode
		self.sourceFile=sourceFile

		self.processImports(schemaNode)
	}
	
	
	self.resolveNamespace = Lambda{
		P(1) as String => name
		error (name.contains("{"), "Already on Clark format!") 
		error (!name.contains(":"), "No namespace prefix!")

		self.schemaNode.namespaceInfo.resolveNamespace(name)
	}

	self.processImports = Lambda{
		P(1) as &XMLNode => schemaNode

		schemaNode.subNodes(SOAP:XSD_IMPORT)->ximp
			ns=ximp.attrGet("namespace")  # must match targetNamespace of schema! (TODO)
			loc=ximp.attrGet("schemaLocation")
			if (loc==null) {
				println("WARNING: ingoring import without schemaLocation attribute")
			} else {
				importFile=self.sourceFile.dir.file(loc)
				println("Processing " + SOAP:XSD_IMPORT + " file: " + importFile.path)

				schema=SOAP:Schema
				XML:Parse(importFile.read).first => newSchemaNode
				XML:ProcessNamespaces(newSchemaNode, self.schemaNode.namespaceInfo)
				
				schema.processSchemaNode(newSchemaNode, importFile, self.dataStore)

				# add the imported node to the document tree
				schemaNode.addSubNode(newSchemaNode)
			}
	}


	# Just verify there are no loose non-namespaced references
	self.checkConsistency = Lambda{
		self.complexTypes.keys->ctname
			self.complexTypes.get(ctname).sequence->ct  # list of Dict with name, type
				name=ct.name
				type=ct.type
				if (!type.contains(":") && !self.complexTypes.has(type) && !self.simpleTypes.has(type)) {
					error(ctName + "." + name + " : UNDEFINED TYPE: " + type)
				}
	}

/class Schema


# Recursively distribute targetNamespace to all XML nodes under
# those that define it, making it available to apply to the
# proper attributes.
# --
	P(1) as &XMLNode => node
	P(2) as String? => targetNamespace

	if (node.attributes.has("targetNamespace")) {
		targetNamespace=node.attrGet("targetNamespace")
	} else if (targetNamespace != null) {
		node.attributes.set("targetNamespace",targetNamespace)
	}
	node.subNodes->x PropagateTargetNamespace(x,targetNamespace)

//PropagateTargetNamespace




# Apply targetNamespace to attributes in elements 
# --
	P(1) as &WsdlDocument => wd
	P(2) as String => tagName
	P(3) as List => attrNames

	wd.rootNode.allSubNodes->n
		assert(n.tagName.endsWith("}"+tagName))
		attrNames->attr
			assert(n.attributes.has(attr))
			value=n.attrGet(attr)
			if (value.contains("{")) {
				# already on Clark format, do nothing
			} else if (value.contains(":")) {
				if (!IsSystemType(value)) value=n.namespaceInfo.resolveNamespace(value)
			} else {
				if (n.attributes.has("targetNamespace")) {
					value="{" + n.attrGet("targetNamespace") + "}" + value
				} else {
					error("Node " + tagName + " has no targetNamespace attribute")
				}
			}
			n.attributes.set(attr,value)
/UseTargetNamespace


# Update various attribute values with regards to targetNamespace
# --
	P(1) as &WsdlDocument => wd

	PropagateTargetNamespace(wd.rootNode)

	# Now all nodes under the WsdlDocument have an attribute targetNamespace if available,
	# below is a list of tag names and attributes which are subject to the targetNamespace,
	# unless containing a regular namespace prefix

	UseTargetNamespace(wd,"element",List("name","value","type","ref"))
	UseTargetNamespace(wd,"complexType",List("name","type"))
	UseTargetNamespace(wd,"simpleType",List("name","type"))
	UseTargetNamespace(wd,"restriction",List("base"))

	UseTargetNamespace(wd,"message",List("name"))
	UseTargetNamespace(wd,"portType",List("name"))
	UseTargetNamespace(wd,"binding",List("name","type"))
	UseTargetNamespace(wd,"service",List("name"))
	#UseTargetNamespace(wd,"operation",List("name"))

	UseTargetNamespace(wd,"part",List("element"))
	UseTargetNamespace(wd,"input",List("message"))
	UseTargetNamespace(wd,"output",List("message"))
	UseTargetNamespace(wd,"fault",List("message"))
	UseTargetNamespace(wd,"port",List("binding"))

/UpdateAttributeValuesNamespaces


# (DEBUG) Locate namespaced and unresolved attributes across the complete document
# For debug purposes
# --
	P(1) as &WsdlDocument => wd
	Lib:Header("Unresolved attributes with namespace prefix in value")
	
	wd.rootNode.allSubNodes->node
		node.attributes.keys->attr
			value=node.attributes.get(attr)
			if (!value.contains("{") && !value.startsWith("http") && value.contains(":")) {
				report(node.tagName, attr, value)
			}
	| _=> rows
	if(rows.empty) {
		println("(None)")
	} else {
		Lib:ShowRows(rows)
	}
/ShowUnresolvedAttributes


# (DEBUG) Show all XML node tags
# --
	P(1) as &WsdlDocument => wd

	Lib:Header("All XML node tags in the document")
	
	dict=Dict
	wd.rootNode.allSubNodes->node
		tag=node.tagName
		count=dict.get(tag,0)+1
		dict.set(tag,count)
	|
	dict.keys->key
		out(Int(dict.get(key),key))
	| _.sort.reverse->x 
		key=x.data
		report(key, dict.get(key))
	| _=> rows
	Lib:ShowRows(rows)
/ShowAllNodeTags


# (DEBUG) Show all combinations of tag + attribute which have
# namespace:value for some but not all instances!
# --
	P(1) as &WsdlDocument => wd

	withNs=Dict
	withoutNs=Dict

	Lib:Header("Show attributes which both have namespace and don't")

	wd.rootNode.allSubNodes->node
		node.attributes.keys->attr
			key=node.tagName+" " + attr
			value=node.attributes.get(attr)
			if (value.contains("{") || value.contains(":")) {
				withNs.set(key,1)
			} else {
				withoutNs.set(key,1)
			}
	|

	# identify shared keys
	withNs.keys->key
		if (withoutNs.has(key)) out(key)
	| => list
	if (list.length==0) {
		println("(None)")
	} else {
		list.print
	}
/ShowAttributesMissingResolve



# (DEBUG) Show attributes without namespace (simple values)
# --
	P(1) as &WsdlDocument => wd

	Lib:Header("Show attributes with simple values")

	dict=Dict
	wd.rootNode.allSubNodes->node
		node.attributes.keys->attr
			value=node.attributes.get(attr)
			if (!value.contains("{")) {
				str=node.tagName + " "+ attr + "=" + value
				dict.set(str,1)
			}
	|
	dict.keys.sort->str println(str)
/ShowNonNamespacedAttributes




# Create SOAP XML call
# --
	P(1,"http://example.com/service") as String => serviceNamespace
	P(2,"noop") as String => action
	P(3,Dict) as Dict => params

	paramList=ActionParameters(params,"ser")
	
	<<< EOF
		<?xml version="1.0" encoding="UTF-8"?>
		<soapenv:Envelope 
			xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/" 
			xmlns:ser="<<serviceNamespace>>">
		<soapenv:Header/>
		<soapenv:Body>
			<ser:<<action>>>
<<paramList>>
			</ser:<<action>>>
		</soapenv:Body>
		</soapenv:Envelope>
	>>> EOF
	_.mergeExpr
/CreateSoapRequest


# Create action parameters (helper)
# --
	P(1) as Dict => params
	P(2) as String => ns
	P(3,"^t^t^t^t".unEsc) => indent

	params.keys->key
		value=""+params.get(key)
		tag=ns + ":" + key
		out(indent + "<"+tag+">" + Util:EscapeXMLString(value) + "</"+tag+">")
//ActionParameters


# Create basic authentication string
# --
	P(1) as String => username
	P(2) as String => password

	(username + ":" + password).getBytes("UTF-8").base64
/BasicAuth	


# Create complete HTTP request for a soap call and call it with CURL:Invoke
# --
	P(1,"http://example.com/service") as String => soapService
	P(2,"http://example.com/service/noop") as String => soapAction
	P(3) as String => body
	P(4) as String? => basicAuth
	P(5, false) => debug

	protocol=soapService.before(":")
	targetHostPort=soapService.after("//").before("/")
	targetHost=targetHostPort.before(":")
	targetService=soapService.after("//").from("/")

	if (debug) {
		println("soapService =",soapService)
		println("targetHostPort=",targetHostPort)
		println("targetHost =",targetHost)
		println("targetService =",targetService)
		println("soapAction =", soapAction)
	}

	headers=Dict
		.set("Content-Type", "text/xml;charset=UTF-8")
		.set("SOAPAction", soapAction)
		.set("Host",targetHost)
		.set("User-Agent","Apache-HttpClient/4.5.5 (Java/12.0.1)")
	
	# Note: Content-Length is handled by curl

	if (basicAuth != null) headers.set("Authorization", "Basic " + basicAuth)

	CURL:Invoke(protocol, targetHostPort, "POST", targetService, headers, body, debug)
/Call

P(1)=host P(2,"get-service -name tomcat")=code
	println("Executing " + code)
	List("powershell","invoke-command","-computername",host,"-scriptblock","{" + code + "}") =cmd
	Dir.run(cmd)
/PSRemote

P(1)=host P(2)=service
	PSRemote(host,"stop-service " + service)
/StopService

P(1)=host P(2)=service
	PSRemote(host,"start-service " + service)
/StartService

P(1)=host P(2)=service
	PSRemote(host,"get-service " + service)
/GetService

